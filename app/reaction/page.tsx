import ReactionTrainer from '@/components/ReactionTrainer';

export const metadata = {
  title: 'Reaction Time — Aim Trainer',
};

export default function ReactionPage() {
  return <ReactionTrainer />;
}
